# tablr
Tablr is an AI-native social layer that turns Dineout into a community engine. Using MCP, users signal dining intent, and the AI performs semantic matchmaking to group people by shared interests, then auto-books reservations via the Dineout API.
